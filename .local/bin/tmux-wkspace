#!/usr/bin/env bash

# Make sure we're inside tmux
if [ -z "$TMUX" ]; then
  echo "This script must be run inside a tmux session."
  exit 1
fi

tmux kill-pane -a

tmux split-window -h
tmux split-window -v

tmux select-layout '3b15,271x77,0,0[271x52,0,0,0,271x24,0,53{90x24,0,53,9,180x24,91,53,13}]'
